---
name: Fred wants automation for client setup
description: Fred wants to turn this into a business — needs a one-command setup script, not a manual guide. Happy to provide API keys/codes for services.
type: feedback
---

The manual setup guide is too much work. Fred wants to be able to spin up a new client site in minutes, not 30 minutes of manual steps.

**Why:** Fred wants to productise this as a service for multiple clients. Manual setup per client doesn't scale.

**How to apply:** Build automation scripts that take API keys as input and do everything — create repos, deploy, set up databases, configure env vars. Fred is comfortable providing service credentials/tokens for Claude to use directly.
